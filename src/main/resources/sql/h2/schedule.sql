DROP table if exists SCHED_GROOVY_JOB;
CREATE TABLE SCHED_GROOVY_JOB (
  ID BIGINT GENERATED BY DEFAULT AS IDENTITY, 
  CODE_CONTENT CLOB, 
  NAME VARCHAR (50)  NOT NULL UNIQUE,
  DESCRIPTION VARCHAR (200),
  PRIMARY KEY (ID)
  );
COMMIT;
